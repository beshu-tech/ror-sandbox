readonlyrest:

  audit:
    enabled: true
    outputs: [index]

  access_control_rules:

    - name: "::KIBANA-SRV::"
      auth_key: kibana:kibana
      verbosity: error

    - name: "::ADMIN::"
      auth_key: admin:admin
      actions: ["*"]
      indices: ["*"]
      verbosity: error
