readonlyrest:

  access_control_rules:

    - name: "KIBANA"
      type: allow
      auth_key: kibana:kibana
      verbosity: error

    - name: "ADMIN"
      type: allow
      auth_key: admin:admin
      kibana_access: admin
      # verbosity: error

    # - name: "User 1"
    #   type: allow
    #   auth_key: "user1:test"
    #   indices: [".kibana*", "my*"]
    #   kibana_access: ro
    #   kibana_index: '.kibana'
    #   # verbosity: error

    # - name: "User 2"
    #   type: allow
    #   auth_key: "user2:test"
    #   indices: [".kibana*", "my*"]
    #   kibana_access: ro
    #   # verbosity: error

    - name: "Example3"
      auth_key: "user3:test"
      indices: [".kibana_tenant1", "my*"]
      kibana:
        access: ro
        index: .kibana_tenant1

    - name: "Example4"
      auth_key: "user4:test"
      indices: [".kibana_tenant1*", "my*"]
      kibana:
        access: ro
        index: .kibana_tenant1